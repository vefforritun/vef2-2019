<%- include('header') -%>

<p>Hjá okkur starfa:</p>
<ul>
  <% staff.forEach((name) => { %>
    <li><%= name %></li>
  <% }) %>
</ul>

<% if (typeof extra !== 'undefined') { %>
  <%- extra %>
<% } %>

<%- include('footer') -%>
